# naver-blog-mcp

네이버 블로그에 서식·이미지 포함 글을 작성하는 MCP 서버. Playwright 기반.

## 지켜야 할 설계 결정

이 결정들은 이유가 있어서 내린 것이다. 바꾸려면 먼저 물어볼 것.

1. **비밀번호를 저장하지 않는다.** `login_setup.py`로 사람이 직접 로그인해
   `playwright-state/storage_state.json`에 쿠키만 덤프한다. 서버는 그 파일만 읽는다.
   CAPTCHA·2차인증은 사람이 처리한다. 로그인 자동화 코드를 추가하지 말 것 —
   봇 탐지에 걸리는 표면이자 계정 제재 리스크다.

2. **셀렉터는 `selectors.py`에만 둔다.** 다른 파일에 셀렉터 문자열을 하드코딩하지 말 것.
   각 항목은 후보 리스트이고 `S.first()`가 위에서부터 시도한다.
   후보 순서는 의미 기반(role/aria-label/텍스트) → 클래스 기반. 네이버 클래스명은 난독화돼 자주 바뀐다.

3. **기본값은 임시저장.** `create_draft`는 발행하지 않는다. `publish_draft(confirm=True)`가 별도다.
   셀렉터가 깨졌을 때 라이브 블로그에 깨진 글이 올라가는 것보다 임시저장함에 쌓이는 게 낫다.

4. **본문 입력은 클립보드 HTML 붙여넣기가 1차 경로.** 스마트에디터 ONE은 HTML 편집 모드가 없지만
   리치 텍스트 붙여넣기는 받는다. 툴바 클릭보다 훨씬 안정적이다.
   실패 시 키보드 단축키 폴백(`type_spans`). 툴바 클릭은 최후수단.

5. **이미지는 붙여넣기 불가.** 툴바 이미지 버튼 → `expect_file_chooser` 가로채기만 가능하다.
   그래서 `ir.segment()`가 본문을 [html | image | html]로 쪼갠다. 로컬 경로만, 10MB 제한.

6. **마크다운에 없는 블록은 `:::name 인자:::` 디렉티브로 쓴다.**
   `:::file 로컬경로:::` / `:::formula x^2+y^2=z^2:::` / `:::place 강남역:::`.
   일정 등도 같은 틀에 얹는다.
   이유: 링크 문법(`[라벨](경로)`)을 재사용하면 진짜 링크와 구분이 모호해 오탐이 난다.
   디렉티브는 표준 마크다운 뷰어에서 그냥 텍스트로 보여 원문이 깨지지 않는다.
   모르는 디렉티브는 버리지 않고 문단으로 강등한다.

## 현재 상태

2026-08-25 실측 완료. `check_session` / `list_categories` / `create_draft` E2E 통과.

- `ir.py` — 검증 완료. `Segment` 에 `blocks` 필드 추가(폴백이 세그먼트 단위로 동작하게).
- `session.py`, `login_setup.py` — 동작 확인.
- `selectors.py` — **8개 항목 전부 실측 확정.** 모두 1순위 후보로 잡힌다.
  후보 순서 원칙이 바뀌었다 (파일 상단 주석 참조): 네이버의 `data-click-area` /
  `data-testid` / `data-name` / `data-a11y-title` 가 가장 오래 간다. 텍스트 매칭은
  마지막에, 그것도 컨테이너로 스코프를 좁힌 뒤에만.
- `editor.py` — 발행 레이어 헬퍼 추가. macOS 단축키(`ControlOrMeta`) 반영.
- `server.py` — mcp 2.0 (`MCPServer`) 기준. 툴 6개:
  `check_session` / `list_categories` / `list_drafts` /
  `create_draft` / `delete_draft` / `publish_draft` / `delete_post`.

### 고친 것

1. `dismiss_popups` 가 매 실행마다 본문 취소선을 켜고 있었다. 스코프 없는
   `button:has-text('취소')` 가 툴바의 **취소선** 버튼을 부분매칭했다.
   (취소선 상태 False→True 로 확인) → `RECOVER_POPUP_CANCEL` 을 `.se-popup` 안으로 한정.
2. `networkidle` 로 기다리면 `TargetClosedError` 가 난다. 네이버 에디터는 연결을
   계속 열어둬서 idle 에 도달하지 않는다 → `goto_editor()` 가 `domcontentloaded` +
   에디터 iframe 명시적 대기를 쓴다.
3. 카테고리/태그는 **발행 설정 레이어 안에만** 존재한다. 레이어를 열지 않던
   `list_categories` / `create_draft(tags=...)` 가 조용히 실패하고 있었다.
4. `check_session` 이 `LOGGED_IN_MARK` 로 판정했는데 그 마크는 로그인 상태에서도
   `visible=False` 다 → `MYBLOG_PROBE_URL` 리다이렉트 판정으로 교체.

### 첨부·삽입 실측 (2026-08-25)

- **이미지** — 툴바 버튼이 곧장 파일 다이얼로그를 띄운다. 업로드 ~1초.
  캡션은 **두 번 클릭**해야 한다: 이미지 컴포넌트 클릭(캡션이 0x0 → 노출) →
  캡션 클릭(포커스). 컴포넌트만 클릭하고 타이핑하면 입력이 아무 데도 안 들어간다.
- **파일 첨부** — 이미지와 다르다. `.se-popup-file` 팝업에서
  "내 컴퓨터"(`data-log='lfile.local'`)를 눌러야 파일 다이얼로그가 뜬다. 파일당 10MB.
  `editor.insert_file()` 구현·검증 완료. 마크다운 문법은 `:::file 경로:::` 로 정했다.
- **표** — 붙여넣기로 그대로 산다. HTML `<table>` → `se-component se-table`.
  GFM 파이프 문법을 `ir.py` 가 파싱하며 셀 안의 굵게/링크도 유지된다.
- 업로드 완료 판정은 **컴포넌트 개수 증가**로 봐야 한다. 존재 검사만 하면
  이미 이미지가 있는 글에서 두 번째 삽입을 안 기다리고 통과한다.
- `.se-popup-file` 이 우리가 처음 만난 실제 `se-popup` 이다.
  `RECOVER_POPUP_CANCEL` 의 `.se-popup` 스코프가 실재하는 클래스 계열임이 확인됐다.

### 붙여넣기 서식 보존 범위 (2026-08-25 실측)

붙여넣기로 **살아남는 것** — `ir.PASTE_SAFE`:

| 마크다운 | 결과 |
|---|---|
| 제목 1~3 | 같은 본문 컴포넌트 안에서 글자 크기로 (se-fs24 / 19 / 15) |
| 굵게 / 기울임 | `<b>` / `<i>` 그대로 |
| 취소선 | `<del>` 로 보내야 산다. **`<s>` 는 살균기가 지운다** |
| 인용 | `se-quotation` 컴포넌트 |
| 구분선 | `se-horizontalLine` 컴포넌트 |
| 표 | `se-table` 컴포넌트. 셀 안의 굵게/링크도 유지 |
| 밑줄 / 배경색 | `<u>`, `background-color` 스타일 모두 유지 |

붙여넣기가 **문단으로 뭉개는 것** — 툴바/키보드로 따로 만든다:

| 대상 | 실패 이유 | 대신 쓰는 경로 |
|---|---|---|
| 목록 | `<ul>/<ol>/<li>` 제거됨 | `"- "` / `"1. "` 타이핑 → 에디터가 자동 변환. **마커는 첫 항목에만** 친다 |
| 코드블록 | `<pre><code>` 제거됨. ``` 타이핑도 안 됨 | 툴바 `data-name='code'` → `textarea.se-code-source-editor` 를 `fill()` |
| 링크 | `<a href>` 제거됨 (라벨≠URL, 라벨=URL, 맨 URL, http 전부) | 텍스트를 친 뒤 Shift+← 로 되선택 → 툴바 `data-name='text-link'` → URL 입력 → 적용 |
| 인라인 코드 | `<code>`, monospace 스타일 모두 제거됨 | **추가하지 않는다.** 네이버에 대응 기능이 없어서 흉내만 낼 수 있고, 그건 안 하기로 정했다 |

커서 취급 주의 (실측으로 재현한 버그들):

- 붙여넣기 전에 컴포넌트를 **다시 클릭하면 안 된다.** 커서가 클릭 지점(글 중간)에
  놓여 내용이 뒤섞인다. 최초 1회만 클릭하고 이후에는 커서를 이어 쓴다.
- 이미지/파일/코드블록은 포커스를 캡션·textarea 로 가져간다. 그 뒤에 생기는
  빈 본문 컴포넌트를 클릭해 되돌아온다 (`_focus_tail`).
- 목록의 `"- "` 자동 변환은 **줄 맨 앞에서만** 걸린다 (`_fresh_line`).
- 붙여넣기 성공 판정은 본문 길이가 아니라 **문서 전체(컴포넌트 수 + 텍스트 길이)**
  로 봐야 한다. 인용구/표/코드는 별도 컴포넌트라 본문은 그대로여서 "무반응" 으로
  오판하고 중복 입력한다.

### 인라인 서식 (2026-08-25 실측)

- 굵게/기울임/밑줄 — `ControlOrMeta+B/I/U` **동작한다**.
- 취소선 — `ControlOrMeta+Shift+S` 는 **동작하지 않는다**. 선택 후 툴바
  `button[data-name='strikethrough']` 를 눌러야 한다.
  적용 뒤에도 토글이 `se-is-selected` 로 **켜진 채 남아** 이후 타이핑에 전부 번진다.
  반드시 꺼줄 것 (`_clear_toggle`).
- 링크 — 결과가 `<a href>` 가 **아니라** `<span class="se-link" data-href="...">` 다.
  `<a>` 로 검증하면 "링크가 안 걸렸다" 고 오판한다. 실제로 그렇게 헛짚었다.
- 링크가 든 **문단과 제목**을 타이핑 경로로 보낸다 (`ir.is_paste_safe`).
- **제목은 글자 크기일 뿐이다.** 스마트에디터 ONE 에는 H1~H3 개념이 없다.
  `text-format` 드롭다운은 본문/소제목/인용구 셋뿐이고, 붙여넣기가 `<h1>/<h2>/<h3>` 을
  `se-fs24/19/15` 로 바꾸는 것이다 (h3 는 본문과 같은 15라 사실상 구분이 없다).
  그래서 제목도 타이핑으로 만들 수 있다: 내용을 치고 → 그 줄을 선택 →
  `font-size` 드롭다운으로 크기 지정. 링크와 제목을 둘 다 살린다.
  크기 모드도 취소선처럼 남으므로 제목 뒤에 본문 크기로 되돌린다.
- 인용/표 안의 링크는 여전히 못 살린다 — 타이핑으로 `se-quotation` / `se-table`
  자체를 만들 수 없어서, 링크를 살리려다 블록을 잃는다. 블록을 지키고 링크를 버린다.

### 수식·장소 (2026-08-25 실측)

- **수식** `:::formula ...:::` — 비주얼 수식편집기(NME)지만 스크립트 입력창
  (`textarea.nme_script_editor`)이 따로 있어 자동화된다. `입력`(`nme_button_submit`)으로 삽입.
  **`fill()` 은 먹지 않는다** — 편집기가 키 입력으로만 스크립트를 파싱한다.
  `press_sequentially` 로 실제 타이핑할 것. 결과는 `se-formula` 컴포넌트.
- **장소** `:::place 검색어:::` — 검색 → 결과의 **"추가"** → 확인.
  결과 링크(`se-place-map-search-result-link`)를 누르는 건 지도 미리보기일 뿐 선택이 아니다.
  "추가" 버튼은 항목에 **hover 해야 보인다**. 추가 전에는 확인 버튼이 `disabled` 다.
  검색 결과가 여러 개면 **첫 번째**를 쓰고, 무엇을 골랐는지 `write_post` 가 notes 에 남긴다.
- **장소 검색은 경쟁 조건이 있다.** 지도 팝업은 React 앱이라 입력창이 보여도 아직
  준비 전일 수 있고, 그때 돋보기(`se-place-search-button`)를 누르면 **검색이 아예
  발동하지 않는다**. 결과가 없으니 "검색 결과가 없습니다" 로 오인하기 딱 좋다.
  쿼리 탓도, 첫 호출 탓도 아니라 간헐적이다 — 같은 쿼리가 됐다 안 됐다 한다.
  그래서 팝업을 기다리고, 타이핑 후 잠깐 쉬고, **결과가 안 뜨면 검색을 다시 누른다**
  (최대 3회). 검색은 결과로만 발동 여부를 알 수 있어서 재시도 말고는 방법이 없다.
- 수식 팝업은 Escape 로 안 닫힌다. 열어둔 채로 다른 툴바 버튼을 누르면
  `se-popup-dim` 이 클릭을 가로챈다.

### 실측으로 확정된 사실

- **붙여넣기 1차 경로가 실제로 동작한다.** headless 에서도 클립보드 HTML 이 붙는다.
  제목/문단/목록/표까지 붙여넣기로 처리된다.
- **이미지·파일 뒤에는 본문 컴포넌트가 새로 생긴다.** `write_post` 가 여기서 타깃을
  갱신하지 않으면 다음 붙여넣기가 처음 본문으로 돌아가 **이미지 위**에 붙는다.
  (실측으로 재현·수정함 — `_last_body()`)
  `write_post()` 가 세그먼트별로 `붙여넣기` / `타이핑` 중 무엇을 썼는지 돌려준다.
  붙여넣기는 예외 없이 조용히 실패할 수 있어서 본문 길이 변화로 판정한다.
- **글쓰기 화면을 열면 임시저장 글이 자동 복구되지 않는다.** 빈 문서가 뜬다.
  복구 팝업도 안 뜬다 (그래서 `RECOVER_POPUP_CANCEL` 은 여전히 미실측).
  발행하려면 임시저장 목록에서 명시적으로 불러와야 한다 → `load_draft()`.
- **임시저장은 카테고리와 태그를 보존한다.** 불러온 뒤 하위 카테고리("안드로이드")와
  태그(#테스트 #셀렉터실측)가 그대로 남아 있는 것을 확인했다.
- 임시저장 목록 항목을 클릭하면 확인 팝업 없이 바로 에디터에 반영된다.
  삭제 버튼은 평소 숨김이고 '편집' 을 눌러야 보인다 (셀렉터만 기록, 미구현).
- `ul[class*='list__']` / `li[class*='item__']` 는 카테고리·공개설정·예약에 공용이다.
  `div[class*='option_category']` 로 스코프를 좁혀야 카테고리 12개만 남는다.
- 발행 레이어는 Escape 로 닫는데, 드롭다운이 열려 있으면 **2번** 필요하다.

### 발행·삭제 (2026-08-25 실측)

- **`publish_draft` 최종 클릭 검증 완료.** 비공개로 설정해서 발행했다
  (비공개로 발행 후 `delete_post` 로 정리함). 발행하면 그 글은 임시저장함에서 빠진다.
- **공개 설정을 제어할 수 있다.** `publish_draft(visibility=...)` —
  `public` / `neighbor` / `both_neighbor` / `private`. 비우면 글에 설정된 값을 쓴다.
  커스텀 라디오라 Playwright 클릭이 안 먹어 JS `element.click()` 으로 누른다.
- **삭제 흐름**: 목록에서 '편집' 을 누르면 항목별 삭제 버튼은 사라지고
  체크박스 선택 모드가 된다 (전체 삭제 / 선택 삭제 / 완료).
  체크박스는 `label` 이 덮고 있어 **label 을 클릭**해야 한다.
  확인이 **네이티브 dialog** 로 뜬다 — Playwright 는 기본적으로 dialog 를 dismiss 하므로
  핸들러를 걸지 않으면 삭제가 조용히 취소되고, 그러고도 예외가 없어 성공처럼 보인다.
- 발행·삭제 모두 `editor.pick_draft` 안전장치를 공유한다 (애매하면 거부).

### 발행된 글 삭제 (2026-08-25 실측)

`delete_post(url_or_log_no, confirm=True)`. 임시저장 삭제와 다른 UI 다.

- 글 페이지(`#mainFrame`)의 `a[class~='_deletePost']`. **텍스트로 찾지 말 것** —
  연관글 제목에 "삭제" 가 들어가면 그것도 잡힌다. `class*=` 도 안 된다
  (`_deletePostConfirm` 까지 잡힌다). 토큰 정확 매칭(`~=`)을 쓴다.
- 확인은 네이티브 dialog: "삭제된 글은 복구할 수 없습니다."
- **대상 확인 가드가 필수다.** 이미 삭제됐거나 없는 글 URL 로 가면 네이버가
  블로그 홈으로 튕기는데, **그 페이지에도 보이는 "삭제" 링크가 있다**
  (`_param(1|false|false)`). 가드 없이 진행하면 엉뚱한 페이지에서 삭제를 누르고
  확인 dialog 까지 자동 수락한다. 그래서 URL 에 대상 logNo 가 있는지 먼저 본다.
  (실제로 이 상태를 만들어 봤고, 다행히 대상이 없어 아무것도 지워지지 않았다.)
- 검증: 새 글 임시저장 → 비공개 발행 → 삭제 → 재삭제 거부까지 확인.

### S.first 의 함정 (2026-08-25)

`S.first` 는 이제 매칭 **전체를 훑어 보이는 것**을 고른다. `.first` 만 보면
숨김 요소가 앞에 올 때 실패한다. 실제로 두 번 걸렸다:

- `PUBLISH_OPEN`: 숨김 "예약 발행 0건" 이 "발행" 보다 앞
- `POST_DELETE`: 숨김 "삭제하기" 가 보이는 "삭제" 보다 앞

`timeout` 은 후보별이 아니라 전체 예산이다. 이 변경 후 `verify_selectors.py`
8개 항목 회귀 없음을 확인했다.

### 남은 문제

- 인용/표 안의 링크는 살릴 수 없다 (블록 형태를 지키고 링크를 버린다).
- 인라인 코드는 **넣지 않기로 했다** — 네이버에 대응 기능이 없다.
- 장소는 검색 첫 결과를 쓴다. 정확히 특정하려면 검색어를 구체적으로 줘야 한다.

### 발행 안전장치

`publish_draft` 와 `delete_draft` 는 애매하면 아무것도 하지 않는다
(`editor.pick_draft`, 브라우저 없이 검증됨). 둘 다 `confirm=True` 를 요구한다:

- `title` 없이 호출 + 임시저장 2건 이상 → 거부하고 목록을 보여준다
- `title` 이 여러 글과 부분 일치 → 거부하고 후보를 보여준다
- 정확 일치가 부분 일치보다 우선
- 불러온 뒤에도 제목이 비어 있으면 발행 중단

### 도움말 패널 (2026-08-27 실측)

**로그인 직후 첫 글쓰기 화면에서 도움말 패널이 자동으로 열리고 툴바 클릭을 통째로 막는다.**
`login_setup.py` 직후 첫 툴 호출이 여기 걸린다. 실제로 재로그인 후 `verify_selectors.py`
가 이것 때문에 터졌다.

- 닫기: `button.se-help-panel-close-button` → `dismiss_popups` 가 처리한다.
- 한 번 열리면 localStorage 의 `se3#HELP_PANEL//STORAGE` 에 `{"alreadyOpened":true}` 가
  기록돼 다시 안 뜬다. **재현하려면 그 키를 지우고 새로고침**하면 된다 (`S.HELP_PANEL_FLAG`).
- 그동안 못 재현하던 "뜨면 닫아야 진행되는 레이어" 의 실제 사례다.
  `RECOVER_POPUP_CANCEL`(작성중 글 복구)은 여전히 미실측.

### 코드리뷰 반영 (2026-08-27)

공개 전 리뷰에서 나온 것들. 대부분 "실패했는데 성공으로 보고" 하는 부류였다.

- **`list_drafts` 인덱스 어긋남** — 제목이 안 읽히는 항목을 건너뛰어서, 그 뒤 항목이
  한 칸씩 밀렸다. 이 인덱스가 그대로 `load_draft`/`delete_draft` 의 DOM 인덱스라
  **엉뚱한 글을 발행하거나 삭제**할 수 있었다. 이제 건너뛰지 않고 `(제목 없음 #n)` 으로 채운다.
- **삭제 dialog 를 아무거나 수락** — "로그인이 필요합니다" 같은 알림도 삼키고
  "삭제 완료" 로 보고했다. 이제 `DELETE_CONFIRM_HINTS` 로 문구를 확인하고,
  아니면 dismiss 한 뒤 실패로 알린다.
- **`_doc_state` 센티널** — 실패를 `(-1,-1)` 로 돌려줘서 두 번 실패하면 서로 같아
  "붙여넣기 무반응" 으로 판정되고 세그먼트를 통째로 다시 타이핑했다(내용 중복).
  이제 `None` 을 돌려주고, 확인 불가일 때는 타이핑하지 않는다.
- **`wait_for_editor` 조용한 타임아웃** — 실패해도 그냥 리턴해서 main_frame 폴백으로
  이어졌고, 결국 "selectors.TITLE 갱신 필요" 라는 엉뚱한 메시지가 나왔다. 이제 명시적으로 실패한다.
- **커서 이동 수를 `len()` 으로 셈** — 결합문자·ZWJ 이모지에서 어긋나 선택이 앞
  텍스트까지 먹었다. `caret_len()` 근사로 교체.
- **`login_setup.py` 가 `NAVER_STATE` 를 무시** — 로그인은 성공하는데 서버는 다른
  경로를 봐서 "세션 파일 없음" 이 떴다. 이제 `session.STATE` 를 공유한다.
- **쿠키 파일이 0644** — 계정 접근권한 그 자체인데 같은 기기의 누구나 읽을 수 있었다.
  이제 디렉터리 0700 / 파일 0600 (`save_state_file`).
- **`Session.__aenter__` 정리 경로 없음** — 컨텍스트 생성이 실패하면 `__aexit__` 이
  안 불려 크로미움과 드라이버가 샜다.
- **쿠키 파일 비원자적 쓰기** — 임시파일 + `os.replace` 로 바꿨다.
- **로그아웃 상태 덮어쓰기** (리뷰 밖에서 실제로 당함) — 로그인 페이지로 튕긴 실행이
  인증 쿠키 없는 상태를 저장해 멀쩡한 쿠키를 날렸다. 이제 `NID_AUT`/`NID_SES` 가
  없으면 기존 파일을 건드리지 않는다.
- **`server.py` 에 `NAVER_BLOG_ID` 검증 없음** — 빈 값이면 셀렉터 탓처럼 보이는
  메시지가 나왔다. `guarded` 데코레이터로 확인하고, Playwright 예외도 문자열로 돌려준다.
- **`verify_selectors.py` 가 프로덕션과 다른 판정 사용** — `.first.is_visible()` 은
  `S.first` 가 버린 방식이라, 실전에서 잘 되는 셀렉터를 MISS 로 오보했다. 이제 `S.first` 를 쓴다.
  취소선 회귀 검사도 툴바가 그려진 뒤에 하고, 버튼을 못 찾으면 조용히 넘어가지 않는다.
  프로브 JS 의 하드코딩 셀렉터도 인자로 받게 바꿨다 (규칙 2).

## 다음 작업 순서

```bash
uv sync && uv run playwright install chromium
uv run python login_setup.py             # 직접 로그인 (uv run 필요)
export NAVER_BLOG_ID=<블로그아이디>
uv run python verify_selectors.py        # 창 띄움
HEADLESS=true uv run python verify_selectors.py   # 창 없이
```

`verify_selectors.py` 는 2단계로 검사한다: 에디터 첫 화면 → 발행 레이어를 열고 나머지.
발행 레이어는 열기만 하고 최종 발행 버튼은 누르지 않는다.
`dismiss_popups` 가 툴바를 건드리는지 회귀 검사도 같이 한다.

1. **붙여넣기 서식 보존 범위를 실측한다.** 붙여넣기 자체는 되는 게 확인됐으니,
   이제 굵게/링크/목록/인용/코드블록 중 무엇이 살고 무엇이 문단으로 뭉개지는지 본다.
   표·이미지·파일은 확인됐고, 인용/코드블록/중첩목록이 남았다.
   뭉개지는 것만 `_type_block` 폴백으로 강제한다.
2. `_SHORTCUT["strike"]` = `"ControlOrMeta+Shift+S"` 미검증.
   툴바에 `button[data-name='strikethrough']` 가 있으므로 단축키가 틀리면 이걸 쓴다.

## 하지 말 것

- `HEADLESS=true` 기본값으로 바꾸지 말 것. CAPTCHA가 뜨면 사람이 풀어야 한다.
- `storage_state.json`, `dom_dump.html` 커밋 금지 (.gitignore에 있음).
- 대량 자동 포스팅 루프 추가 금지. 네이버 약관 위반이고 계정이 날아간다.
